syntax = "proto3";

package greeter;
option go_package = "/proto;proto";

// Service
service Greeter {
  rpc SayHello (HelloRequest) returns (HelloReply);
}

// Messages
message HelloRequest {
  string name = 1;
}

message HelloReply {
  string message = 1;
}